# 拼豆字母组合网页：设计说明

## 1. 目标

做一个自用的本地网页：输入单词或句子，自动用已有的字母图纸拼成拼豆图纸，并导出带网格线的 JPG 图片。

## 2. 数据来源：字母图纸.xlsx

- 文件只有一张 `Sheet1`，用 Excel 黑色实心填充画字母。
- 已确认里面有两套完整字母表：
  - 第 3-29 行：小写 a-z
  - 第 33-59 行：大写 A-Z
- 小写字母按 xlsx 里横排对齐后的位置提取，`g/j/p/q/y` 的下伸尾巴包含在各自字母范围内。
- 解析脚本会生成 `letters.js`，网页直接读取这份数据。

以后想换字母样式：

- 把新的字母图纸按相同布局放进新的 xlsx，替换后重跑 `build_letters.py` 即可。
- 也可以直接替换 `letters.js`。
- 网页逻辑不需要改，所以换样式很方便。

## 3. 网页输入变量

1. 单词 / 句子：支持手动换行，在输入框里按 Enter 就会产生新的一行拼豆图。
2. 字母间隔：两个字母之间插入几个空白格。
3. 单词间隔：两个单词之间插入几个空白格。
4. 大小写模式：
   - 全部大写
   - 第一个单词首字母大写
   - 全部单词首字母大写
   - 全部小写
5. 颜色：默认黑色。
6. 行间距：手动换行后，行与行之间插入几个空白格，默认 0，可调。
7. 字母样式：经典、粗体。
8. 包边：无、仅包边、一行长方体、完全长方体。
9. 描边宽度和描边颜色可单独设置。
10. 行缩进：按当前行数显示缩进输入框，每行可单独设置。
11. 插入符号：星、月、心，支持小、中、大三档。

大小写规则按行应用。例如选择“第一个单词首字母大写”时，每一行手动换行后的首字母都会大写。

## 4. 拼接规则

- 字母宽度按图纸原样保留，不统一成 5 格。`I/L` 这类窄字母保持窄，`M` 保持 7 格宽。
- 字母间隔指两个字母实际边界之间插入的空白列数。
- 单词间隔指单词之间额外插入的空白列数；输入里的空格只作为单词分隔，不占字母位置。
- 大小写混排时按基线对齐：
  - 大写字母高 7 格，基线在底部。
  - 小写字母以 x-height 底部作为基线。
  - `g/j/p/q/y` 的下伸部分从基线继续往下画。
- 当前不支持标点符号；遇到不支持字符直接跳过，空格仍按单词分隔处理。

## 5. 输出格式

JPG 为主：

- 白色背景。
- 图纸四周自动加 5 格空白边距。
- 每个格子画细分割线，浅灰色。
- 每 5 格画粗分割线，深灰色，形成 5x5 方格。
- 导出时按较高分辨率生成，保证打印清晰。

Excel 本期不做，后续需要时再加。

## 6. 技术方案

文件结构：

- `index.html`：单页网页，无依赖，双击打开即可使用。
- `letters.js`：由 xlsx 生成的 52 个字母数据。
- `build_letters.py`：从 xlsx 重新生成 `letters.js`，用于以后更换字母样式。

实现方式：

- 每个字母存成 0/1 矩阵，并附带宽度、高度、基线信息。
- `letters.js` 里包含多套样式：经典、粗体。
- 网页用 Canvas 按行拼接字母、插入间隔、绘制网格线。
- 通过 Canvas 导出 JPG 文件。
- 包边按方案 B 实现：三种模式都会填充同一行内和已对齐行之间的空隙，区别在填充范围。

### 移动端兼容说明

- 夸克、UC、QQ 等手机浏览器可能无法直接保存 blob 或 base64 下载，已改用英文文件名 `pindou-pattern.jpg` / `pindou-pattern.png`。
- 如果下载仍然失败，点“打开图片”会在页面里显示图片，手机上长按图片即可保存。

## 7. 使用方法

- 双击打开 `index.html` 即可使用，不需要安装或联网。
- 输入单词或句子后点“生成”查看预览。
- 点“下载 JPG”保存带网格线的图纸。
- 网页里也提供“下载 PNG”，需要无损图时可以使用。
- 以后更换字母样式：替换 `字母图纸.xlsx` 后重跑 `python build_letters.py`。

## 8. 已确认的决定

- 保留原字母宽度。
- 按基线对齐大小写。
- 手动换行由用户自己控制。
- 标点先不支持。
- JPG 为主，能用 JPG 就不做 Excel。
- 网格线使用默认的白底浅灰细线、深灰粗线。

## 9. 实现状态

- 已完成：`build_letters.py` 提取 52 个字母数据并生成 `letters.js`。
- 已完成：`index.html` 支持拼接、预览、手动换行、JPG/PNG 导出。
- 已验证：示例句子能正常生成，JPG 下载可用。
- 已完成：网页支持在经典、粗体之间切换。
- 已完成：支持标点、星月爱心符号，以及每行缩进设置。
- 已完成：支持三种包边模式、描边宽度和描边颜色。
- 待做：手写风格需要一份手写字母模板（和现有 `字母图纸.xlsx` 类似的 Excel），直接转换字体到 5x7 格子的效果辨识度太差。

## 10. 字母修正记录

- 2026-08-03：按用户提供图案修正小写 `w`、小写 `t`、小写 `y`、大写 `Q`。
- 小写 `w` 当前按用户最终版，最后一行是 `oxoxo`。
- 修正写在 `build_letters.py` 的 `PATTERN_OVERRIDES` 中，重新生成数据时不会丢失。

## 11. 发布为网页链接

当前网页是纯静态文件，不需要改代码。发布时必须把 `index.html` 和 `letters.js` 放在同一个文件夹里一起上传。

### 方式一：Netlify Drop，最快

1. 打开 <https://app.netlify.com/drop>。
2. 建议先免费注册并登录 Netlify，这样链接可以长期保留。
3. 把 `拼豆` 文件夹直接拖进页面，或者只拖 `index.html` 和 `letters.js` 两个文件。
4. 等上传完成后，Netlify 会给一个类似 `https://xxxx.netlify.app` 的链接。
5. 打开这个链接即可使用，之后可以直接分享给别人。

以后修改了字母或页面，重新拖一次文件夹就能更新。

### 方式二：GitHub Pages，适合长期维护

1. 在 GitHub 新建一个仓库。
2. 把 `index.html` 和 `letters.js` 上传到仓库。
3. 在仓库 Settings 里的 Pages 中，选择 Deploy from a branch，分支选 main，目录选 root。
4. 保存后会自动生成 `https://你的用户名.github.io/仓库名/`。

两种方式都不需要改网页代码。
